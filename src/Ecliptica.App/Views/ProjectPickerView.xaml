<UserControl x:Class="Ecliptica.App.Views.ProjectPickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource DeepSpaceBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Simulation Project Selection" FontSize="24" Foreground="{StaticResource StarWhiteBrush}" FontWeight="Bold" Margin="40,30,40,10"/>

        <!-- Main Content split -->
        <Grid Grid.Row="1" Margin="40,10,40,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- Projects List -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Select Project" FontSize="14" Foreground="{StaticResource StarWhiteBrush}" Margin="0,0,0,8" FontWeight="SemiBold"/>
                <ListBox Grid.Row="1" ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}" MouseDoubleClick="ListBox_MouseDoubleClick">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding FilePath}" FontSize="11" Foreground="{StaticResource MutedTextBrush}" Margin="0,3,0,0"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding LastModifiedAt, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource MutedTextBrush}"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button Grid.Row="2" Content="Delete Selected" Command="{Binding DeleteProjectCommand}" Style="{StaticResource StandardButtonStyle}" Margin="0,10,0,0" HorizontalAlignment="Right"/>
            </Grid>

            <!-- New Project Creator -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Create New Project" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <TextBlock Text="Project Name" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewProjectName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
                    <Button Content="Create Project" Command="{Binding CreateProjectCommand}" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Navigation Bar -->
        <Border Grid.Row="2" Background="{StaticResource SpaceNavyBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="40,15">
            <Grid>
                <Button Content="← Back to Home" Command="{Binding BackCommand}" Style="{StaticResource StandardButtonStyle}" HorizontalAlignment="Left"/>
                <Button Content="Next Workspace →" Command="{Binding NextCommand}" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
