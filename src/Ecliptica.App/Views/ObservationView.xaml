<UserControl x:Class="Ecliptica.App.Views.ObservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource DeepSpaceBrush}">
        <!-- Viewport Workspace Placeholder -->
        <Grid>
            <TextBlock Text="[ Observation Mode Viewport (OpenGL Canvas Placeholder) ]" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       FontSize="20" 
                       Foreground="{StaticResource MutedTextBrush}"/>

            <!-- Top Floating Exit Button -->
            <Button Content="Exit Observation" 
                    Command="{Binding ExitCommand}" 
                    Style="{StaticResource StandardButtonStyle}" 
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Top" 
                    Margin="20"/>

            <!-- HUD Overlay Panel -->
            <Border HorizontalAlignment="Right" 
                    VerticalAlignment="Stretch" 
                    Width="320" 
                    Background="#CC05060F" 
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1,0,0,0" 
                    Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="Celestial Explorer" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource NebulaPurpleBrush}"/>
                        <TextBlock Text="Explore real stellar catalogs &amp; data" FontSize="11" Foreground="{StaticResource MutedTextBrush}" Margin="0,3,0,0"/>
                        <Separator Margin="0,15"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Object Information" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,15"/>
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Selected Object: None" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <TextBlock Text="Right-click on objects to select and observe." FontSize="11" Foreground="{StaticResource MutedTextBrush}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <TextBlock Grid.Row="2" Text="Observation mode has no save requirements." FontSize="11" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
