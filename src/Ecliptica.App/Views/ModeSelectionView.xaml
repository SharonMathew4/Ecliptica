<UserControl x:Class="Ecliptica.App.Views.ModeSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource DeepSpaceBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Header -->
        <StackPanel Grid.Row="0" Margin="0,50,0,0">
            <TextBlock Text="ECLIPTICA" 
                       FontSize="36" 
                       HorizontalAlignment="Center"
                       Foreground="{StaticResource StarWhiteBrush}"
                       FontWeight="Bold"/>
            <TextBlock Text="Select Operation Mode" 
                       FontSize="14" 
                       Foreground="{StaticResource MutedTextBrush}" 
                       HorizontalAlignment="Center" 
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Mode Buttons / Cards -->
        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <!-- Simulation Mode Card -->
            <Button Grid.Column="0" Command="{Binding EnterSimulationCommand}" Style="{StaticResource StandardButtonStyle}" Height="200" Padding="0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <!-- Visual representation using path or symbol -->
                    <Viewbox Grid.Row="0" Width="48" Height="48" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,15">
                        <Path Fill="{StaticResource AccentBlueBrush}" Data="M12,18A6,6 0 1,1 18,12A6,6 0 0,1 12,18M20,8.69V4H15.31L12,0.69L8.69,4H4V8.69L0.69,12L4,15.31V20H8.69L12,23.31L15.31,20H20V15.31L23.31,12L20,8.69Z"/>
                    </Viewbox>
                    <TextBlock Grid.Row="1" Text="Simulation Mode" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="{StaticResource StarWhiteBrush}"/>
                    <TextBlock Grid.Row="2" Text="Sandbox mode for N-body gravity, collisions, and stellar physics." Margin="0,10,0,0" FontSize="12" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                </Grid>
            </Button>

            <!-- Observation Mode Card -->
            <Button Grid.Column="2" Command="{Binding EnterObservationCommand}" Style="{StaticResource StandardButtonStyle}" Height="200" Padding="0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Viewbox Grid.Row="0" Width="48" Height="48" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,15">
                        <Path Fill="{StaticResource NebulaPurpleBrush}" Data="M12,9A3,3 0 0,0 9,12A3,3 0 0,0 12,15A3,3 0 0,0 15,12A3,3 0 0,0 12,9M12,4.5C7,4.5 2.73,7.61 1,12C2.73,16.39 7,19.5 12,19.5C17,19.5 21.27,16.39 23,12C21.27,7.61 17,4.5 12,4.5M12,17A5,5 0 0,1 7,12A5,5 0 0,1 12,7A5,5 0 0,1 17,12A5,5 0 0,1 12,17Z"/>
                    </Viewbox>
                    <TextBlock Grid.Row="1" Text="Observation Mode" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="{StaticResource StarWhiteBrush}"/>
                    <TextBlock Grid.Row="2" Text="Explore real stellar catalogs, galaxies, and planetary ephemerides." Margin="0,10,0,0" FontSize="12" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                </Grid>
            </Button>
        </Grid>

        <!-- Exit Button -->
        <Button Grid.Row="2" 
                Content="Exit Application" 
                Command="{Binding ExitApplicationCommand}" 
                Style="{StaticResource StandardButtonStyle}" 
                HorizontalAlignment="Center" 
                Margin="0,0,0,50"/>
    </Grid>
</UserControl>
