<Window x:Class="Ecliptica.App.Views.Dialogs.UnsavedChangesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Unsaved Changes" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource CosmicGradient}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="Save Changes?" FontSize="18" FontWeight="SemiBold" Style="{StaticResource TitleTextStyle}" Margin="0,0,0,10"/>
            <TextBlock Text="You have unsaved changes in your project. Would you like to save them before exiting?" FontSize="12" TextWrapping="Wrap" Foreground="{StaticResource StarWhiteBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save &amp; Exit" Click="Save_Click" Style="{StaticResource AccentButtonStyle}" Margin="0,0,10,0"/>
            <Button Content="Discard Changes" Click="Discard_Click" Style="{StaticResource StandardButtonStyle}" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="Cancel_Click" Style="{StaticResource StandardButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
